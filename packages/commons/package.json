{
  "name": "@repo/commons",
  "version": "0.0.0",
  "type": "module",
  "_main": "./src/index.ts",
  "_types": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "files": [
    "dist"
  ],
  "private": true,
  "license": "MIT",
  "scripts": {
    "bundle": "tsup-node",
    "bundle:watch": "tsup-node --watch",
    "type:gen": "tsc --emitDeclarationOnly",
    "type:gen:watch": "tsc --emitDeclarationOnly --watch",
    "clear:output": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "pnpm clear:output && run-p bundle type:gen",
    "dev": "run-p bundle:watch type:gen:watch",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:type": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@turbo/gen": "2.9.14",
    "@types/node": "24.6.2",
    "@types/react": "19.2.0",
    "@types/react-dom": "19.2.0",
    "npm-run-all": "4.1.5",
    "tsup": "8.5.0",
    "vitest": "4.1.7"
  }
}
