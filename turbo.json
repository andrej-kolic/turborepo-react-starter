{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local", "prettier.config.js"],

  "globalEnv": [
    "BUNDLER",
    "BUILD_ENVIRONMENT",
    "NODE_ENV",
    "NODE_DEBUG",
    "MODE",
    "APP_REACT_ENV_FILE",
    "APP_REACT_TITLE",
    "APP_REACT_VERSION"
  ],
  "globalPassThroughEnv": ["GITHUB_TOKEN"],

  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "preview": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "check:type": {
      "dependsOn": ["^build", "^check:type"]
    },
    "check:type:go": {
      "dependsOn": ["^build", "^check:type:go"]
    },
    "test:watch": {
      "cache": false
    },
    "clear:output": {
      "cache": false
    }
  }
}
